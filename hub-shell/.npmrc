# GitHub Packages для scope @ukituki-ps (april-tokens / april-ui).
# Токен не хранится в репозитории: задайте NODE_AUTH_TOKEN (PAT read:packages или GITHUB_TOKEN в CI).
@ukituki-ps:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
