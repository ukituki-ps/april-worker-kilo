# План: Hub Testing Completion (Этап 008)

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-15
- **Статус плана:** согласован

## Исходные допущения
- Этапы `001-007` завершены; `hub-bff` остаётся stateless BFF-агрегатором без собственного persistence-контура.
- В репозитории уже есть baseline проверки: `make openapi-lint`, `cd hub-bff && go test ./...`, `./scripts/smoke-aprilhub.sh`.
- `hub-shell` уже использует Vitest, но test-run должен быть закреплён как обязательная проверка в CI.
- k6 baseline должен быть воспроизводим локально/в CI без перехода к production performance tuning.

## Порядок работ (шаги)
1. Актуализировать тестовый контур документационно: зафиксировать этап `008` как нетривиальную задачу с отдельным планом и отчётом.
2. Усилить CI-контур тестов:
   - добавить job выполнения `hub-shell` unit tests,
   - добавить job запуска k6 baseline для `hub-bff`.
3. Добавить воспроизводимый k6 baseline:
   - сценарий нагрузки на ключевые BFF endpoint-ы,
   - скрипт-раннер с поднятием smoke runtime (Keycloak + hub-bff), получением токена и запуском k6 в docker.
4. Прогнать обязательные проверки этапа `008` и зафиксировать фактические результаты.
5. Обновить task-артефакты (`TASK.md`, `REPORT.md`) и статусы в `task_list.md`.

## Затрагиваемые области
| Область | Что меняется (кратко) |
|--------|------------------------|
| Backend (Go) | Изменений runtime-кода нет; используется существующее покрытие `go test` |
| Frontend | CI-добавление запуска `hub-shell` unit tests (Vitest) |
| БД / Atlas | Не меняется |
| Инфра / Compose | Добавляется тестовый k6 runner поверх существующего smoke-profile |
| Документация / OpenAPI | Обновление task-артефактов этапа `008` и `task_list.md` |

## Риски и откат
- **Риск:** нестабильность k6 baseline из-за холодного старта сервисов в compose. → **Митигация:** ожидание health/openid readiness перед запуском k6.
- **Риск:** рост времени CI из-за новых job. → **Митигация:** короткий baseline-профиль нагрузки, ограниченный по времени.
- При необходимости отката: удалить k6 runner/job и вернуть прежний CI; приложение/контракты не меняются.

## Проверка после выполнения
- Команды: `make openapi-lint`, `cd hub-bff && go test ./...`, `cd hub-shell && npm ci && npm run test`, `./scripts/smoke-aprilhub.sh`, `./scripts/run-k6-aprilhub.sh`.
- Ручная проверка: убедиться, что auth/RBAC/aggregation сценарии остаются без регрессий.

## Примечания
- Связанные артефакты: `docs/TESTING_STRATEGY.md`, `.github/workflows/ci.yml`, `scripts/smoke-aprilhub.sh`.
- Обновления плана: 2026-04-15 — первичная фиксация и реализация.
