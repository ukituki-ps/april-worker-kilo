# План: актуализация раздела Profiles list widget после phase 5

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-28
- **Статус плана:** согласован

## Исходные допущения
- Внешние изменения выполнены в `april-profile-1` в задаче `040-phase-5-widget-card-layout-modernization`.
- В текущем репозитории scope ограничен документацией и task-story материалами, без расширения runtime-реализации `hub-shell`.
- Основной артефакт синхронизации для людей и агентов: `docs-site/docs/task-story-026-phase-4a-hub-profiles-list-host-bff-flow.md`.

## Порядок работ (шаги)
1. Зафиксировать исходный scope и ограничения в `PLAN.md`.
2. Актуализировать `docs-site`-разделы по `Profiles list widget` с упором на phase 5 и cross-repo синхронизацию.
3. Добавить отдельную task-story запись для внешней задачи `040` и связать ее с обзорным индексом.
4. Прогнать проверку сборки `docs-site`.
5. Подготовить `REPORT.md` по шаблону с фактами, проверками и ограничениями.

## Затрагиваемые области
| Область | Что меняется (кратко) |
|--------|------------------------|
| Backend (Go) | Не меняется |
| Frontend | Не меняется |
| БД / Atlas | Не требуется |
| Инфра / Compose | Не меняется |
| Документация / OpenAPI | Обновление `docs-site` task stories + отчёт задачи |

## Риски и откат
- **Риск:** внешний репозиторий недоступен в текущем workspace, поэтому нельзя автоматически сверить конкретные коммиты phase 5.  
  **Митигация:** в документации явно указать, что изменения синхронизированы по внешней задаче и требуют прямой сверки при ревью в `april-profile-1`.
- **Риск:** возможное расхождение runtime-реализации `hub-shell` и внешнего UI-пакета.  
  **Митигация:** фиксировать такие точки в `REPORT.md` как follow-up, без несанкционированного расширения scope.
- **Откат:** реверт изменений в `docs-site/docs/*`, `tasks/041.../PLAN.md`, `tasks/041.../REPORT.md`, `task_list.md`.

## Проверка после выполнения
- `npm --prefix docs-site run build`
- Ручная проверка ссылок в `docs-site/docs/task-stories-overview.md` и новой task-story страницы `040 phase 5`.

## Примечания
- По задаче деплой не требуется.
- При появлении локального доступа к `april-profile-1` можно дополнить отчёт ссылками на конкретные внешние коммиты/PR.
