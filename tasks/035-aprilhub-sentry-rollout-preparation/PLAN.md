# План: подготовка внедрения Sentry в AprilHub

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-25
- **Статус плана:** согласован

## Исходные допущения

- Этап `035` ограничен документацией и env-контуром без runtime-изменений `hub-shell`/`hub-bff`.
- Архитектурный baseline Sentry + Loki/Prometheus уже зафиксирован в `docs/architecture/ERROR_TELEMETRY_MODEL.md`; здесь нужен операционный rollout-пакет для реализации в задаче `037`.
- Реальные DSN и production-секреты не хранятся в git; репозиторий содержит только шаблонные значения и инструкции.

## Порядок работ (шаги)

1. Зафиксировать план задачи и целевые артефакты (env, redaction/filtering, runbook, implementation checklist).
2. Добавить Sentry env-контур в `.env.example` и документировать назначение переменных.
3. Подготовить runbook rollout/rollback/smoke с правилами sampling, redaction и filtering.
4. Обновить документационные entrypoint-индексы, чтобы runbook был обнаружим агентом и инженерами.
5. Обновить `TASK.md` (acceptance) и подготовить `REPORT.md` по шаблону.

## Затрагиваемые области

| Область | Что меняется |
|--------|----------------|
| Frontend | Нет runtime-изменений; только будущий контур env для `hub-shell` |
| Backend (Go) | Нет изменений |
| БД / Atlas | Нет изменений |
| Инфра / Compose | Шаблон env (без изменения compose flow) |
| Документация | Новый runbook + обновление индексов + отчёт |

## Риски и откат

- **Риск:** некорректные default sample rates могут привести к шуму/стоимости после реализации `037` → **Митигация:** фиксируем консервативные стартовые значения и явный post-rollout review.
- **Риск:** утечка чувствительных данных в telemetry → **Митигация:** фиксируем redaction policy и deny-list до начала внедрения SDK.
- Откат: revert документа/шаблона env; runtime-состояние не затрагивается.

## Проверка после выполнения

- Ручная проверка ссылок и согласованности `TASK.md` ↔ runbook ↔ `REPORT.md`.
- Dry-run команд smoke/rollback из runbook без реального `SENTRY_DSN`.

## Примечания

- Связанные задачи: `033`, `034`, `037`.
