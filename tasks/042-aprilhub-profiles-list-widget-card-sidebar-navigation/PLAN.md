# План: открыть Profiles list widget-card из сайдбара

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-28
- **Статус плана:** согласован

## Исходные допущения
- Пункт `Профиль — список` в sidebar уже привязан к маршруту `#/app/profile/entities`, но сценарий открытия `widget-card` нужно явно подтвердить и зафиксировать тестом.
- Основной слой изменений: `hub-shell` (routing + UI smoke-test в unit/integration тестах фронтенда).
- Изменения БД и backend-контрактов не требуются.

## Порядок работ (шаги)
1. Проверить текущий путь sidebar `Профиль — список` и обработчик маршрута `profile-list` в `hub-shell`.
2. Доработать `ProfilesListHostWidget` для явной идентификации `widget-card` (удобно для smoke/e2e).
3. Добавить/актуализировать тест в `hub-shell`, подтверждающий сценарий: клик по sidebar -> открывается `Profiles list widget-card`.
4. Прогнать проверки `lint` и `test` для `hub-shell`.
5. Оформить `REPORT.md` с результатами, рисками и follow-up.

## Затрагиваемые области
| Область | Что меняется (кратко) |
|--------|------------------------|
| Backend (Go) | Не меняется |
| Frontend | `hub-shell`: маркер карточки виджета + тест навигации sidebar |
| БД / Atlas | Не меняется |
| Инфра / Compose | Не меняется |
| Документация / OpenAPI | Отчёт задачи `tasks/042.../REPORT.md`; OpenAPI не меняется |

## Риски и откат
- **Риск:** регрессия тестов из-за моков API в `App.test.tsx`. → **Митигация:** добавить отдельный mock `authorizedFetch`, не ломая существующие сценарии.
- **Риск:** расхождение ожиданий UI-текста в тесте. → **Митигация:** опираться на `data-testid` карточки виджета.
- При необходимости отката: удалить изменения в тестах/`data-testid`, поведение маршрутизации останется прежним.

## Проверка после выполнения
- Команды:
  - `npm --prefix hub-shell run lint`
  - `npm --prefix hub-shell run test -- --runInBand`
- Ручная проверка / smoke:
  - В авторизованной зоне нажать `Профиль — список` в sidebar.
  - Убедиться, что отображается карточка `Profiles list widget` (host-flow и route работают).

## Примечания
- Связанные задачи: `041` (документационная синхронизация phase-5) и `042` (реализация сценария sidebar -> widget-card).
