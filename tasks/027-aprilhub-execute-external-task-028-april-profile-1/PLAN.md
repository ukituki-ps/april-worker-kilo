# План: исполнение внешней задачи 028 из april-profile-1

- **Задача:** [`TASK.md`](./TASK.md)
- **Дата плана:** 2026-04-24
- **Статус плана:** согласован

## Исходные допущения
- Источник требований: `april-profile-1/tasks/028-phase-4a-hub-instances-host-routing-e2e/TASK.md`.
- В `april-worker` уже реализованы host-shell и BFF/OIDC контур для профиля (задачи 021/026/035), но маршрут экземпляра пока заглушка.
- Выполнение должно включать человекопонятную историю в `docs-site` и двойной отчёт в двух репозиториях.

## Порядок работ (шаги)
1. Зафиксировать scope задачи 028 и синхронизировать его с текущим состоянием `hub-shell`.
2. Реализовать host-экран экземпляров на маршруте `/app/profile/instances/:instanceId` с BFF-префиксом `/api/v1/admin/profile/api`.
3. Добавить/обновить smoke e2e сценарий: логин, переход в раздел экземпляров, одна CRUD-операция через BFF.
4. Обновить документацию `docs-site` (story + overview) по сценарию экземпляров и ручным ограничениям.
5. Прогнать релевантные проверки (`hub-shell` lint/test + Playwright smoke) и зафиксировать результаты.
6. Подготовить итоговый отчёт в `april-profile-1/tasks/028-.../REPORT.md` и дублирующий отчёт в `tasks/027-.../REPORT.md`.

## Затрагиваемые области
| Область | Что меняется (кратко) |
|--------|------------------------|
| Backend (Go) | Нет изменений |
| Frontend | `hub-shell`: маршрутизация/экран экземпляров, e2e smoke |
| БД / Atlas | Нет изменений |
| Инфра / Compose | Нет изменений |
| Документация / OpenAPI | `docs-site` story + overview, отчёты по задаче |

## Риски и откат
- **Риск:** нестабильность внешнего стенда (Keycloak/BFF/Profile) влияет на e2e. → **Митигация:** использовать детерминированные Playwright route stubs на BFF-префиксе и фиксировать ручные предпосылки.
- **Риск:** расхождение фактов между двумя отчётами. → **Митигация:** заполнять отчёты из одного набора данных (команды, файлы, ограничения).
- При необходимости отката: реверт изменений `hub-shell` и документации без влияния на backend/данные.

## Проверка после выполнения
- `cd hub-shell && npm run lint`
- `cd hub-shell && npm run test`
- `./scripts/run-playwright-aprilhub.sh`

## Примечания
- Связанные материалы: `april-profile-1/tasks/028-phase-4a-hub-instances-host-routing-e2e/TASK.md`.
- Обновления плана: 2026-04-24 — создан baseline-план выполнения задачи 028 в `april-worker`.
